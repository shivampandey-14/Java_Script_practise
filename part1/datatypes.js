// comment

/*multi line comment
diffrent data type in js
string
number
boolean

object
bigint

undefined
null
*/
let size = 100; //let is used to define the variable size is name of the variable.
console.log(size);
size = "hii";
console.log(size);
//values can be changed but if we add const in the begning then values will not change it will give error.
const x = 100;
// x = 200; will give error.
let user = { name: "shivam", lastname: "pandey" };//just like dictionary or map
let tup = ['hii', 2]; //just like tuple.
console.log(tup);